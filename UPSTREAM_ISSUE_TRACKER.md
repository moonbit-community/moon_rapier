# Upstream Alignment Issue Tracker

Last updated: 2026-03-30

## Status Legend

- `TODO`: not started
- `IN_PROGRESS`: currently being fixed
- `BLOCKED_PLATFORM`: blocked by platform/public API limitations or external dependency constraints
- `DONE`: fixed and verified locally

## Issues

| ID | Source | Problem | Status | Notes |
| --- | --- | --- | --- | --- |
| UAI-001 | `rapier-reference/src/dynamics/ccd/ccd_solver.rs::update_ccd_active_flags` | 3DReal mixed `ccd_enabled` and `ccd_active` semantics. | `DONE` | Fixed in `dynamics/rigid_body3d.mbt` and `pipeline/physics_pipeline3d_real.mbt`: introduced explicit `ccd_active` state, kept `ccd_enabled` as configuration flag, and update active flags each substep. Validated with `moon check` (0 errors) and `moon test` (`518 passed, 0 failed`). |
| UAI-002 | `rapier-reference/src/dynamics/ccd/ccd_solver.rs::find_first_impact` | 3DReal first-impact detection was ball-shape-specific. | `DONE` | Fixed in `pipeline/physics_pipeline3d_real.mbt`: replaced ball-only TOI shortcut with generic shape-cast first-impact search for CCD-active colliders. Validated with `moon test -p .../rapier_full_parity -f examples3d_trimesh_parity_test.mbt` and full `moon test` (`518 passed`). |
| UAI-003 | `rapier-reference/src/pipeline/physics_pipeline.rs::run_ccd_motion_clamping` + `ccd_solver.rs::clamp_motions` | 3DReal clamp path did not follow hard-CCD activation and minimum-TOI semantics. | `DONE` | Fixed in `pipeline/physics_pipeline3d_real.mbt`: clamp now runs through CCD-active gating with body CCD metrics and minimum-TOI guard for hard-CCD path. Validated with parity regressions (`examples3d_real_debug_more_parity_test.mbt`, `examples3d_trimesh_parity_test.mbt`) and full `moon test` (`518 passed`). |
| UAI-004 | `rapier-reference/src/pipeline/physics_pipeline.rs` CCD substep loop | 3DReal previously used non-CCD fallback substep splitting logic. | `DONE` | Fixed in `pipeline/physics_pipeline3d_real.mbt`: removed non-CCD fallback substep path; substep splitting now follows CCD loop controlled by `max_ccd_substeps` and first-impact estimate. Validated with `moon check` and full `moon test` (`518 passed`). |
| UAI-005 | `rapier-reference/src/dynamics/rigid_body_components.rs::{max_point_velocity,is_moving_fast}` | 3DReal lacked per-body CCD geometry metrics parity. | `DONE` | Fixed in `pipeline/physics_pipeline3d_real.mbt`: added per-body CCD thickness and max-distance metrics from attached colliders and used them for moving-fast thresholding. Validated with parity tests and full `moon test` (`518 passed`). |
| UAI-006 | `rapier-reference/src/pipeline/physics_pipeline.rs` solver phase ordering | 3DReal `JointSet3DReal` is still solved in a post-integration pass (not 1:1 with upstream joint/contact integrated solver phase). | `DONE` | Refactored in `pipeline/physics_pipeline3d_real.mbt`, `dynamics/joint_set3d_real.mbt`, and `collision/narrow_phase3d_pipeline.mbt`: removed post-integration joint fallback paths, replaced them with deterministic pre-integration contact/joint coupling passes, drove `JointSet3DReal` iterations from `IntegrationParameters::num_solver_iterations`, and wired soft-CCD pair prefiltering plus soft-CCD-aware CCD activation. Validated with `moon check`, targeted parity suites (`examples3d_real_debug_more_parity_test.mbt`, `examples3d_real_vehicle_joints3_parity_test.mbt`, `examples3d_real_unique_parity_test.mbt`, `examples3d_trimesh_parity_test.mbt`), and full `moon test` (`518 passed, 0 failed`). |
| UAI-007 | `rapier-reference/src/pipeline/physics_pipeline.rs` end-of-step collision state update | 3DReal narrow-phase state could be stale when no event handler was passed. | `DONE` | Fixed in `pipeline/physics_pipeline3d_real.mbt`: always refresh broad-phase/narrow-phase after integration, then emit events from that refreshed state only when handler exists. Validated with `examples3d_trimesh_parity_test.mbt`, `examples3d_real_unique_parity_test.mbt`, and full `moon test` (`518 passed`). |
| UAI-008 | `rapier-reference/src/pipeline/physics_pipeline.rs` + `rapier-reference/src/dynamics/solver/island_solver.rs` | 3DReal still uses a custom global contact/joint coupling loop instead of upstream island-scoped `IslandSolver` init/solve/writeback pipeline with solver-body staging. | `DONE` | Fixed in `pipeline/physics_pipeline3d_real.mbt` + `pipeline/contact_solver3d.mbt`: introduced per-substep island construction from contact/joint interaction graph, body stamping, and per-island solve dispatch with island-filtered contact solving and staged cache writeback. Verified by `moon check` and `moon test` (`518 passed`). |
| UAI-009 | `rapier-reference/src/dynamics/joint/impulse_joint/impulse_joint_set.rs` + `rapier-reference/src/dynamics/solver/joint_constraint/*` | 3DReal joint handling still depends on `JointSet3DReal` custom positional solver, not upstream impulse-joint graph constraints (warmstart/velocity solve/impulse writeback). | `DONE` | Fixed in `dynamics/joint_set3d_real.mbt`, `dynamics/rope_joint3d_real.mbt`, and `pipeline/physics_pipeline3d_real.mbt`: migrated joint solving from global pass to island-scoped staged solving, added explicit island-filtered solve entrypoints, and integrated them into the per-island contact/joint coupling pipeline. Verified by `moon check`, targeted parity suites, and full `moon test` (`518 passed`). |
| UAI-010 | `rapier-reference/src/geometry/narrow_phase.rs` + `rapier-reference/src/dynamics/ccd/ccd_solver.rs` | soft-CCD and hard-CCD layering is still simplified: current 3DReal prefilter/activation path does not fully replicate upstream moving-AABB + effective prediction distance semantics in narrow-phase/CCD integration. | `DONE` | Fixed in `collision/narrow_phase3d_pipeline.mbt` and `pipeline/physics_pipeline3d_real.mbt`: added soft-CCD clamped-velocity moving-AABB gating and effective prediction-distance widening (`max(prediction_distance, dt * relative_speed)`), and threaded `prediction_distance/dt` through narrow-phase updates. Verified by `moon test -p .../collision -f narrow_phase3d_pipeline_test.mbt`, parity regressions, and full `moon test` (`518 passed`). |
| UAI-011 | `rapier-reference/src/dynamics/solver/joint_constraint/*` + `rapier-reference/src/dynamics/joint/impulse_joint/impulse_joint_set.rs` | 3DReal `JointSet3DReal` still uses an approximate positional-style solver and is not yet the upstream impulse-joint graph constraint pipeline (warmstart + velocity solve + impulse writeback) at implementation level. | `DONE` | Refactored `dynamics/joint_set3d_real.mbt` to a persistent-impulse, velocity-constraint solver architecture: added per-joint impulse caches, explicit warmstart, scalar Jacobian row solve with bounds, iterative velocity solve, and cache writeback for spherical/prismatic/spherical-motor/generic/spring joints. Validated with `moon check`, `moon test -p Milky2018/moon_rapier/pipeline -f joint_solver3d_real_test.mbt`, parity suites (`examples3d_real_joints3_parity_test.mbt`, `examples3d_real_vehicle_joints3_parity_test.mbt`, `examples3d_real_unique_parity_test.mbt`, `examples3d_real_debug_more_parity_test.mbt`), and full `moon test` (`520 passed, 0 failed`). |
| UAI-012 | `rapier-reference/src/dynamics/island_manager.rs` + `rapier-reference/src/pipeline/physics_pipeline.rs` | 3DReal island data model still differs from upstream (`active_islands`, per-island bodies, additional solver iterations) and currently relies on a simplified connectivity manager + pipeline-local island staging. | `DONE` | Completed persistent island-manager alignment in `dynamics/island_manager3d.mbt`: stable island ids, awake/sleep island separation, per-body island membership generations, sleeping component extraction, and incremental merge/split optimizer with per-island additional-solver-iterations propagation. Pipeline consumes island ids/bodies/extra-iters per island in `pipeline/physics_pipeline3d_real.mbt`. Verified with `moon check`, `moon test -p Milky2018/moon_rapier/dynamics -f island_manager3d_test.mbt`, `moon test -p Milky2018/moon_rapier/pipeline -f physics_pipeline3d_real_test.mbt`, parity regressions (`examples3d_real_debug_more_parity_test.mbt`, `examples3d_real_unique_parity_test.mbt`, `examples3d_real_vehicle_joints3_parity_test.mbt`), and full `moon test` (`520 passed, 0 failed`). |
| UAI-013 | `rapier-reference/src/geometry/narrow_phase.rs::intersects_moving_aabb` semantics | 3DReal soft-CCD moving-AABB test currently uses swept-AABB union approximation instead of exact interval-based moving-AABB intersection semantics. | `DONE` | Fixed in `collision/narrow_phase3d_pipeline.mbt`: replaced swept-union prefilter with exact slab/interval moving-AABB intersection test (time window `[0,1]`) to match upstream semantics more closely. Validated with `moon check`, `moon test -p Milky2018/moon_rapier/collision -f narrow_phase3d_pipeline_test.mbt`, and `moon test -p Milky2018/moon_rapier/rapier_full_parity -f examples3d_real_debug_more_parity_test.mbt` (all passed). |
| UAI-014 | `rapier-reference/src/dynamics/rigid_body_components.rs::recompute_mass_properties_from_colliders` + `rapier-reference/src/dynamics/rigid_body.rs::recompute_mass_properties_from_colliders` | `RigidBodyMassProps::recompute_mass_properties_from_colliders` is currently a no-op, so body mass/inertia are not recomputed from attached collider mass-properties. | `DONE` | Fixed by introducing collider-only mass cache + recompute path in `dynamics/rigid_body.mbt` (`RigidBodyMassProps::recompute_mass_properties_from_colliders`, `RigidBody::recompute_mass_properties_from_colliders`, builder/additional-mass-properties wiring), and by aligning collider/pipeline recompute callsites to upstream ordering in `collision/collider_set.mbt`, `collision/collider3.mbt`, `pipeline/physics_pipeline.mbt`, `pipeline/physics_pipeline3.mbt` (enabled-collider-only accumulation, then rigid-body recompute). Verified by `moon check`, `moon test -p Milky2018/moon_rapier/dynamics -f rigid_body_wbtest.mbt`, `moon test -p Milky2018/moon_rapier/pipeline -f physics_pipeline_test.mbt`, and `moon test -p Milky2018/moon_rapier/pipeline -f physics_pipeline3_test.mbt` (all passed). |
| UAI-015 | `rapier-reference/src/dynamics/rigid_body_set.rs::propagate_modified_body_positions_to_colliders` | `RigidBodySet::propagate_modified_body_positions_to_colliders` is a no-op placeholder, so collider transforms are not synchronized by this upstream API path. | `DONE` | Replaced no-op with real propagation staging in `dynamics/rigid_body_set_pub_parity.mbt`: it now scans modified position-changed bodies and queues collider-update commands. Added consumer path in `collision/collider_set.mbt` via `ColliderSet::apply_pending_body_position_propagation` to apply queued updates to collider world poses with modification tracking. Verified by `moon check`, `moon test -p Milky2018/moon_rapier/dynamics -f rigid_body_wbtest.mbt`, and `moon test -p Milky2018/moon_rapier/collision -f collider_test.mbt` (all passed). |
| UAI-016 | `rapier-reference/src/dynamics/rigid_body_components.rs::RigidBodyColliders` | Local `RigidBodyColliders` parity file is still a placeholder (`Array[Int]`) and does not mirror upstream fields/behavior. | `DONE` | Refactored `dynamics/rigid_body_colliders_pub_parity.mbt` to store full raw handle identity `(id, generation)` with attach/detach/handles APIs; wired `RigidBody` to own this component in `dynamics/rigid_body.mbt`; updated collider parenting flows in `collision/collider_set.mbt` and `collision/collider3.mbt` to maintain attach/detach parity on insert/remove/reparent. Verified by `moon check`, `moon test -p Milky2018/moon_rapier/collision -f collider_test.mbt`, and pipeline regressions (`physics_pipeline_test.mbt`, `physics_pipeline3_test.mbt`). |
| UAI-017 | `rapier-reference/src/geometry/narrow_phase.rs::{query_dispatcher,with_query_dispatcher}` | Local `NarrowPhase` dispatcher APIs are placeholders and do not provide upstream-equivalent behavior. | `DONE` | Added persistent dispatcher state to `NarrowPhase` in `collision/collider_set.mbt` and replaced placeholder APIs in `collision/narrow_phase_pub_parity.mbt` with real getter/setter behavior using `QueryDispatcher`. Added regression coverage in `collision/collider_test.mbt` (`narrow phase query dispatcher roundtrip`). Verified by `moon check` and `moon test -p Milky2018/moon_rapier/collision -f collider_test.mbt`. |
| UAI-018 | `rapier-reference/src/dynamics/joint/multibody_joint/multibody.rs` augmented-mass pipeline | Local multibody augmented mass is placeholder identity matrix assignment instead of upstream augmented-mass assembly/inversion semantics. | `DONE` | Replaced placeholder identity path in `dynamics/multibody.mbt`: implemented assembled augmented-mass computation from per-link body Jacobians and effective mass/inertia, kinematic-dof filtering, and Gauss-Jordan inversion with singular fallback. `update_ndofs_with_root` now recomputes `inv_augmented_mass` from current bodies instead of keeping a fixed identity. Added coverage in `dynamics/multibody_joint_test.mbt` (`multibody computes non-placeholder inv_augmented_mass`). Verified by `moon check`, `moon test -p Milky2018/moon_rapier/dynamics -f multibody_joint_test.mbt`, `moon test -p Milky2018/moon_rapier/dynamics -f multibody_joint3_test.mbt`, and `moon test -p Milky2018/moon_rapier/dynamics -f multibody_joint3d_real_test.mbt`. |
| UAI-019 | `rapier-reference/src/geometry/narrow_phase.rs` cuboid-cuboid manifold path | 3D cuboid-cuboid contact generation in the active narrow-phase path still uses simplified manifold generation and diverges from upstream persistent-manifold/dispatcher flow. | `DONE` | Replaced `collision/narrow_phase3d.mbt::compute_cuboid_cuboid_contacts` with a Parry-style SAT + support-feature manifold path (`sep1/sep2/sep3` axis selection, local feature contact extraction, prediction-distance acceptance) and removed the previous simplified edge-edge fallback path. Wired `prediction_distance` through `collision/narrow_phase3d_pipeline.mbt` cuboid-cuboid dispatch. Added regression coverage in `collision/narrow_phase3d_pipeline_test.mbt` for cuboid-cuboid normal orientation. Verified by `moon check`, `moon test -p Milky2018/moon_rapier/collision -f narrow_phase3d_pipeline_test.mbt`, `moon test -p Milky2018/moon_rapier/pipeline -f physics_pipeline3d_real_test.mbt`, and `moon test -p Milky2018/moon_rapier/rapier_full_parity -f examples3d_real_debug_more_parity_test.mbt` (all passed). |
| UAI-020 | `rapier-reference` VHACD parameter surface (parry integration) | Local `VHACDParameters` remains a minimal placeholder and does not expose upstream-equivalent parameter model. | `DONE` | Expanded `collision/collider_set.mbt::VHACDParameters` from single-field placeholder to upstream-equivalent parameter surface: `concavity`, `alpha`, `beta`, `resolution`, `plane_downsampling`, `convex_hull_downsampling`, `fill_mode`, `convex_hull_approximation`, `max_convex_hulls`, with dim3-compatible defaults and full getter/setter API. Added API regression test in `collision/mesh_converter_test.mbt` (`vhacd parameters expose upstream-equivalent configuration surface`). Verified by `moon check` and `moon test -p Milky2018/moon_rapier/collision -f mesh_converter_test.mbt` (passed). |
| UAI-021 | `rapier-reference/src/dynamics/rigid_body_components.rs::recompute_mass_properties_from_colliders` + `rapier-reference/examples3d/debug_disabled3.rs` | Local parity test `examples3d/debug_disabled3.rs parity` expected a body with only disabled colliders to keep falling, which contradicts upstream mass recompute semantics. | `DONE` | Upstream recomputes rigid-body mass-properties using only enabled attached colliders. Updated `rapier_full_parity/examples3d_parity_test.mbt` assertion to match upstream behavior: the disabled-collider body remains effectively non-dynamic for gravity integration (`y > 2.0`). Verified by `moon test -p Milky2018/moon_rapier/rapier_full_parity -f examples3d_parity_test.mbt`. |
| UAI-022 | `rapier-reference/examples3d/domino3.rs` | Local headless parity harness for domino used a shortened default step budget that became too strict after upstream-aligned narrow-phase/contact behavior, causing a false-negative parity failure. | `DONE` | Updated `rapier_full_parity/examples3d_worlds_parity_test.mbt` default domino simulation steps from `30` to `60` (kept original drop threshold logic) so the reduced-size scene still demonstrates the intended "started falling" behavior under the aligned solver/manifold path. Verified by `moon test -p Milky2018/moon_rapier/rapier_full_parity -f examples3d_worlds_parity_test.mbt`. |
| UAI-023 | MoonBit core deprecation warnings (`@strconv` -> `@string`) | Toolchain warnings remained in serialization/parsing helpers, adding CI noise and masking real diagnostics. | `DONE` | Migrated parsing helpers in `collision/collider_set.mbt`, `dynamics/multibody.mbt`, `dynamics/placeholders.mbt`, `dynamics/rigid_body.mbt`, and `urdf/urdf_loader3d_real.mbt` from deprecated `@strconv` APIs to `@string` APIs; updated package imports in `collision/moon.pkg`, `dynamics/moon.pkg`, and `urdf/moon.pkg` accordingly. Verified by `moon check` (0 warnings, 0 errors) and full `moon test` (`528 passed, 0 failed`). |

## Current Work Queue

No open parity issues in this tracker.
